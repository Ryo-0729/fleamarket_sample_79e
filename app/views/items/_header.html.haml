.Header
  .Header__top
    = link_to root_path, class: "Logo" do
      = image_tag("logo.png", alt: "logo", class: "Logo__image")
    
    = form_with url: root_path, method: :get, class: "Search", local:true do |form|
      = form.text_field :keyword, placeholder: "キーワードから探す", class: "Search__inputText"
      = form.submit  value: "",class: "Search__submitBtn", type:"submit"

  .Header__bottom
    = link_to '全ての商品一覧', root_path ,class: "Product-table"
    .User
    - if user_signed_in?
      %h2
        現在 #{current_user.nickname} さんがログインしています
      = link_to "ログアウト", destroy_user_session_path, method: :delete
    - else
      %h2 現在ログインしていません
      = link_to 'ログイン', new_user_session_path ,class: "User__login"
      = link_to '新規会員登録', new_user_registration_path ,class: "User__registration"